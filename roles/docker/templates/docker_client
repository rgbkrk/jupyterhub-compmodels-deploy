DOCKER_OPTS="--icc=false --tlsverify --tlscacert={{ docker_tls_path }}/ca.pem --tlscert={{ docker_tls_path }}/cert.pem --tlskey={{ docker_tls_path }}/key.pem -H=0.0.0.0:2376 -H=unix:///var/run/docker.sock"
